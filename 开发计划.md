1.添加一个表ITEM_HANDLER表，他可以关联多个子ITEM，表里面存放LLM的模型信息，可供用户配置
2.添加一个ITEM_HANDLER和用户的权限表，多对多，表示哪些用户可以管理哪些HANDLER
3.添加一个ITEM_HANDLER和ITEM的关联表，多对多，表示一个模型可以被多个ITEM使用，一个ITEM也可以被多个模型使用
4.完善ITEM模型设计，参考MCSManager节点管理子进程的方式，支持ITEM的生命周期管理和状态监控

# ITEM_HANDLER相关表设计

## ITEM_HANDLER表
```sql
CREATE TABLE ITEM_HANDLER (
    ID VARCHAR(32) PRIMARY KEY,
    NAME VARCHAR(255),
    MODEL VARCHAR(255),
    API_KEY VARCHAR(255),
    API_URL VARCHAR(255),
    CREATED_AT TIMESTAMP,
    UPDATED_AT TIMESTAMP
);
```

## ITEM_HANDLER_USER表
```sql
CREATE TABLE ITEM_HANDLER_USER (
    ITEM_HANDLER_ID VARCHAR(32),
    USER_ID VARCHAR(32),
    PRIMARY KEY (ITEM_HANDLER_ID, USER_ID),
    FOREIGN KEY (ITEM_HANDLER_ID) REFERENCES ITEM_HANDLER(ID),
    FOREIGN KEY (USER_ID) REFERENCES USER(ID)
);
```

## ITEM_HANDLER_ITEM表
```sql
CREATE TABLE ITEM_HANDLER_ITEM (
    ITEM_HANDLER_ID VARCHAR(32),
    ITEM_ID VARCHAR(32),
    PRIMARY KEY (ITEM_HANDLER_ID, ITEM_ID),
    FOREIGN KEY (ITEM_HANDLER_ID) REFERENCES ITEM_HANDLER(ID),
    FOREIGN KEY (ITEM_ID) REFERENCES ITEM(ID)
);
```

# ITEM模型设计（参考MCSManager节点管理子进程方式）

## 核心思想
参考MCSManager节点管理子进程的架构，ITEM模型将具备以下能力：
- ITEM作为主节点，可以管理多个子ITEM（类似MCSManager节点管理多个服务器实例）
- 支持ITEM的完整生命周期管理（创建、启动、停止、重启、删除）
- 实时状态监控（运行状态、资源使用情况）
- 配置管理与版本控制
- 日志收集与分析

## ITEM表结构设计
```sql
CREATE TABLE ITEM (
    ID VARCHAR(32) PRIMARY KEY,
    TITLE VARCHAR(255) NOT NULL,
    DESCRIPTION VARCHAR(255),
    OWNER_ID VARCHAR(32) NOT NULL,
    STATUS VARCHAR(32) NOT NULL DEFAULT 'stopped', -- stopped, running, starting, stopping, error
    TYPE VARCHAR(32) NOT NULL DEFAULT 'normal', -- normal, parent, child
    PARENT_ID VARCHAR(32),
    CONFIG JSON, -- 存储ITEM的配置信息
    RESOURCE_USAGE JSON, -- 存储实时资源使用情况
    LOG_PATH VARCHAR(255), -- 日志文件路径
    CREATED_AT TIMESTAMP,
    UPDATED_AT TIMESTAMP,
    FOREIGN KEY (OWNER_ID) REFERENCES USER(ID),
    FOREIGN KEY (PARENT_ID) REFERENCES ITEM(ID)
);
```

## ITEM状态管理

### 状态类型
- `stopped`: 已停止
- `running`: 运行中
- `starting`: 启动中
- `stopping`: 停止中
- `error`: 错误状态

### 生命周期管理API
- `POST /items/{id}/start`: 启动ITEM
- `POST /items/{id}/stop`: 停止ITEM
- `POST /items/{id}/restart`: 重启ITEM
- `DELETE /items/{id}`: 删除ITEM

## 资源监控

### 监控指标
- CPU使用率
- 内存使用率
- 磁盘使用率
- 网络流量
- 进程数量

### 监控机制
- 实时采集资源使用数据
- 支持设置资源阈值告警
- 历史数据存储与分析

## 与MCSManager架构的对应关系

| MCSManager | TermMan |
|------------|---------|
| Node（节点） | ITEM |
| 节点管理进程 | ITEM Handler |
| 服务器配置 | ITEM Config |
| 服务器状态 | ITEM Status |
| 资源监控 | ITEM Resource Usage |