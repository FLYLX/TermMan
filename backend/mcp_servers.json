{
  "mcpServers": {
    "local": {
      "command": "python",
      "args": ["-m", "app.services.agent.mcp.local_server"],
      "enabled": true,
      "description": "本地工具 - execute_command, read_log, memory"
    },
    "robot": {
      "command": "python",
      "args": ["-m", "app.services.agent.mcp.robot_server"],
      "enabled": true,
      "description": "Robot messaging - send messages to current or explicit NoneBot/NapCat QQ conversations"
    }
  }
}
