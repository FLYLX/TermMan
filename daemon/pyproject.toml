[tool.poetry]
name = "termman-daemon"
version = "0.1.0"
description = "TermMan Daemon Service"
authors = ["Author <author@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.104.1"
socketio = "^5.10.0"
python-dotenv = "^1.0.0"
uvicorn = "^0.24.0"
shellingham = "^1.5.4"

[tool.poetry.dev-dependencies]
pytest = "^7.4.3"
black = "^23.11.0"
flake8 = "^6.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
